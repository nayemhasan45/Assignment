#include<stdio.h>
int main()
{
    long long int first,second;
    long long int total;
    scanf("%lld \n%lld",&first,&second);
    total=first*second;
    // printf("%d%d",first,second);
    printf("%lld",total);

    return 0;
}